version: "3.9"

services:
  credit-risk-api:
    build: .
    container_name: bati-bank-credit-risk-api
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    command: uvicorn src.api.main:app --host 0.0.0.0 --port 8000